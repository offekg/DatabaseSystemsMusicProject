import React from "react";
import Typography from "@material-ui/core/Typography";

export default function Welcome() {
  return <Typography>Please select a query to show it's arguments</Typography>;
}
